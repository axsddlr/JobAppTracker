[project]
name = "jobapptracker"
version = "0.1.0"
description = "Job application tracker with Python job scraping"
requires-python = ">=3.11"
dependencies = [
    "jobspy>=0.1.0",
    "pandas>=2.0.0",
]

[dependency-groups]
dev = []

[tool.uv]
link-mode = "copy"
